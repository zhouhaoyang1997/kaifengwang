<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kf.mapper.TipMapper" >
    <insert id="insertTip" parameterType="com.kf.pojo.Tip">
        INSERT into tip(user_id,pi_id,tip_content) VALUES (#{userId},#{piId},#{tipContent})
    </insert>
</mapper>